
import { h } from 'preact';
import { useState, useMemo, useEffect } from 'preact/hooks';

export const CalendarView = ({ id }) => {
    return (<div>
        <h1>VR ERP events calendar</h1>
        <h3>* Cuddlepuddles * ERP * Stripping events * UWU *</h3>
        <br />
    </div>)
};

